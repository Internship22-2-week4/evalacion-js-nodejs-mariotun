export class Photo {
    constructor (photo) {
      // this._id = null
      this._name = photo.name
      this._uri = photo.uri
      this._description = photo.description
    }
  }
  
  //export default Photo